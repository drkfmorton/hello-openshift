apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: hello-openshift
  name: route-unsecured-hello-openshift
spec:
  port: 8090
    targetPort: web
  to:
    kind: Service
    name: hello-openshift
    weight: 100
  tls:
    termination:
  wildcardPolicy: None
